# FlyndMe

PWA que encuentra el destino más barato para un grupo que vuela desde varios orígenes (multi-origen → mejor destino común). Optimiza por coste total o equidad. Beta funcional EN PRODUCCIÓN con datos reales. Última actualización: 2026-06-25.

## Orquestación (lee esto primero: eres el cerebro del proyecto)

Tú (la sesión principal) eres el orquestador. No implementas directamente el trabajo grande: lo delegas en los agentes de `.claude/agents/` y coordinas e integras sus resultados.

**Objetivo de Diego (jun-2026): PRODUCTO REDONDO primero.** Antes que ingresos o crecimiento, quiere una app rematada: rediseño Stitch mergeado y pulido, UX cuidada y precios fiables. Cuando dudes entre opciones, elige la que acerque a eso.

**Estilo de trabajo por defecto: rápido y funcional.** Shippear pronto e iterar; deuda técnica aceptable. Excepción innegociable: todo lo que toque precios, datos o confianza del usuario se hace con rigor (tests + validación), nunca rápido.

**Flujo de delegación:** flyndme-backend / flyndme-frontend implementan (en paralelo si no se pisan) → flyndme-qa valida SIEMPRE antes de cualquier push → flyndme-release commitea, pushea y verifica el deploy. flyndme-producto solo para decisiones de negocio/roadmap.

**Pregunta a Diego antes de:** mergear a main, borrar código no trivial, cambios visuales grandes no pedidos, y cualquier cosa que toque variables de producción (Render/Vercel). Para lo demás, decide tú y explica por qué.

## Stack
- **Frontend**: React + Vite + **Bootstrap (subset Sass: solo los componentes en uso, ver `src/styles/bootstrap-custom.scss`)** + CSS propio (NO Tailwind). Deploy: Vercel → flyndme2.vercel.app (flyndme.vercel.app está caído, no usar).
- **Backend**: Node + Express. Deploy: Render → flyndme-backend.onrender.com (keep-alive vía GitHub Actions cron */10).

## Proveedor de datos de vuelos
- **Travelpayouts/Aviasales Data API en producción** (`FLIGHT_PROVIDER=travelpayouts`). Precios = caché de búsquedas reales, NO ofertas verificables → `verificationStatus: "skipped"` y badge "precio orientativo". Fallback fechas vecinas: `TP_DATE_FLEX_DAYS=2`, mostrando siempre la fecha real.
- **Amadeus**: eliminado por completo el 11-jun-2026 (código del repo y variables de Render). No queda nada pendiente de la migración.
- **Capa 2 (SerpAPI Google Flights, 250 búsquedas gratis/mes)**: endpoint `POST /api/flights/verify` (backend/services/serpapiService.js) — el frontend verifica el GANADOR en segundo plano tras pintar resultados y el badge pasa de "orientativo" a ✓/↑↓. **Desde 16-jun (`mergeVerification`): cuando la verificación es COMPLETA (status `verified`/`changed` = todos los legs verificados), el precio verificado PASA A SER el precio mostrado** (total, por viajero, spread, fairness y price/totalForOrigin de cada leg); el orientativo previo se guarda en `cached*` y el badge `changed` enseña el "antes €X" en el tooltip. En `partial`/`failed`/`timeout` NO se promociona (un total mezcla no es verificado). Nunca se re-ordena (solo se verifica el ganador). **ACTIVA en prod** (`SERPAPI_KEY` en Render desde 11-jun-2026; verificado: FCO 27€ confirmado por Google Flights). OJO: Google Flights NO acepta códigos de ciudad (ROM/LON/PAR → 200 "no results"), por eso se verifica contra el aeropuerto REAL del billete (`offer.tp.originAirport/destinationAirport`, con fallback al código de ciudad). Sin `SERPAPI_KEY` en Render responde `skipped` y todo queda como antes. Va aparte de /multi-origin a propósito: SerpAPI tarda 10-20s y el proxy de Render corta a ~30s. Quota guard doble (contador local + GET /account cacheado 10 min, margen 10), caché por tramo 60 min y por payload 30 min. Logs `[serpapi-verify]` registran la desviación cached vs Google por tramo, con `dateFallback=true/false`.
- Interfaz de servicio común: `getCheapestOffer / priceFlightOffer / budgetStatus` (los providers son drop-in, el frontend no cambia).

## Comandos
- Tests backend: `cd backend && npm test` (~91 tests, sin red, con mocks).
- Tests frontend: `cd frontend && npm test` (~59 tests, harness SSR propio en `tests/_loader.mjs`, incluye render completo de la App).
- Dev frontend: `cd frontend && npm run dev` · Build: `npm run build`.
- **Smoke visual local con datos mock** (para QA estética con capturas): backend `cd backend && USE_MOCK=true PORT=5000 node index.js` + frontend apuntando a él `cd frontend && VITE_API_BASE_URL=http://localhost:5000 npm run dev`. Deep-link que PRERRELLENA el formulario: `http://localhost:5173/?o=MAD&o=LON&o=BER&dep=2026-09-15&trip=oneway` (no auto-busca: hay que pulsar el `button[type=submit]`). Tema por `localStorage.flyndme_theme=dark|light`. Para capturas que el modelo pueda leer: `npm i -D puppeteer` temporal (headless, deep-link + click submit + `waitForSelector('.rv-tab')`), y **quitarlo antes de commitear** (no debe quedar en el repo). El mock muestra placeholders de imagen (las fotos reales solo en prod) y NO renderiza el CTA de Travelpayouts.
- **Verificar deploys de Vercel por CONTENIDO, no por hash**: los hashes de bundle de Vercel ≠ build local (mismo código, distinto entorno). Hacer `curl` del `index.html`/bundle de prod y `grep` de una cadena única del cambio (clase CSS nueva, copy nueva, etc.).

## Estructura clave
- `backend/services/`: travelpayoutsService.js (activo), serpapiService.js (verificación capa 2), mockFlightService.js (USE_MOCK=true). TtlCache, quota guard mensual, rate limiting.
- `backend/routes/`: flights.js (/multi-origin, /verify, /cheaper-date), share.js (resultados compartidos + OG meta), **groups.js (planificación colaborativa de grupo, `/api/groups`, store in-memory)**.
- `frontend/src/`: App.jsx (~1.350 líneas tras extraer componentes y podar imports muertos), SearchPage.jsx, WinnerCard.jsx, FlightResults.jsx, UiBits.jsx, Landing.jsx, ResultsPanels.jsx (incluye CostSplitCard), DestinationMap.jsx (mapa SVG con geodatos reales Natural Earth en europeGeo.js, regenerable con `frontend/scripts/build-map-geo.mjs`), hooks/ (useFocusTrap), utils/ (resultsLogic, urlParams, verification), styles/ (theme-stitch.css, results-simple.css, bootstrap-custom.scss = subset Sass de Bootstrap), CompareChart.jsx (pestaña Comparar = ranking de barras, ya no scatter), ConvergenceHero.jsx (firma del hero), GroupPlanner.jsx (planificación colaborativa de grupo, vista `?group=<id>`), cityImages.js, i18n EN/ES.

## Diseño vigente (tema Stitch)
Granate #AE2F34 / coral #FF6B6B / lavanda (#FCF8FF fondo, #EEECFF contenedores) / azul #0059B8 / verde #00B179. **Tipografía**: Plus Jakarta Sans (UI/cuerpo) + **Bricolage Grotesque** como display solo en el H1 del hero (`--font-display`). **Iconos**: `lucide-react` (set SVG coherente, `currentColor`) — NO emojis en la UI (quedan emojis solo en bloques i18n muertos no renderizados: testimonials/trustBadges/example). **Hero**: diagrama de convergencia (varios orígenes → punto de encuentro, `ConvergenceHero.jsx`, **ESTÁTICO** salvo el pulso infinito del destino; los arcos/nodos NO animan la entrada — cualquier animación de entrada SVG repintaba mal de forma intermitente en Chromium, ver fix del 22-jun). Precios con `tabular-nums`. **Solo 2 pantallas**: home (hero+form+cómo funciona+FAQ) y resultados (lista sobria estilo Skyscanner, clases `altl-*`). En jun-2026 se podaron ~45 widgets con datos inventados: NO resucitarlos. **Modo oscuro completo** (familia navy/lavanda #131434/#1B1C40/#2C2D52, granate aclarado #FFB3B0 con texto tinta #16173B): toggle en header + `prefers-color-scheme`, anti-flash inline en index.html, localStorage `flyndme_theme`. Contraste AA en ambos temas (medido); un solo control de ordenación ("Más barato | Más equitativo" en WinnerCard).

## Reglas duras
1. **Nunca inventar precios ni fingir verificación.** Sin datos → sin resultados. No fabricar aeropuertos de escala.
2. **Coste 0 en APIs durante el MVP**: respetar quota guard, caché agresiva, verificación solo para finalistas.
3. MVP: cambios incrementales, no rewrites; simplicidad antes que arquitectura bonita.
4. Todo cambio de backend lleva tests unitarios sin red.
5. sw.js (PWA) solo se registra con `import.meta.env.PROD` (en dev causó pantallas en blanco).
6. i18n: editar los JSON a mano, nunca reformatear programáticamente.
7. Tras mover código de App.jsx, verificar TODOS los imports (ya hubo dos pantallas en blanco por referencias rotas que los tests SSR no cazaron) → tests + arrancar dev y mirar.
8. No commitear node_modules ni crear symlinks dentro del repo (`git ls-files | grep node_modules` debe estar vacío).
9. `.claude/` está en .gitignore (los agentes son locales; copia maestra en `Fyndme\claude-agents\`).

## Monetización (estado 11-jun-2026)
Afiliación Travelpayouts/Aviasales, marker **738121**. Circuito COMPLETO: `buildAffiliateLink()` (con tests) + CTA "Reservar" desplegados, `TRAVELPAYOUTS_MARKER=738121` activo en Render y cuenta Travelpayouts activada. Verificado en prod con búsqueda real: todos los deep links llevan `&marker=738121`. Pendiente solo esperar las primeras reservas (comisión ~1,1-1,3%) — vigilar el dashboard de Travelpayouts. Nota API: la respuesta de /multi-origin anida los offers en `flights[].flights[].offer.link`.

## Backlog (ordenado según el objetivo "producto redondo")
**ESTADO al 25-jun-2026 (empezar por aquí):** producto redondo y pulido + funciones nuevas en prod. Main en `725680d`. La sesión del 25-jun tuvo dos partes (todo en prod, verificado por contenido). PRIMERO una tanda de pulido: (a) **fix OG con fuente latina completa** — la imagen OG al compartir ahora renderiza el **€ real** (ya no el truco "EUR") y ciudades con acento/latin-ext (Málaga, Düsseldorf, Kraków, Łódź) sin tofu; (b) **cohesión de interacciones** — azul residual del tema viejo (#0062E3) en hover/focus/pulso → color de marca theme-aware (nuevo token `--primary-rgb`) + press `:active` en los botones de reserva; (c) **barrido completo del azul residual** (14 tintes de fondo/borde/selección → token de marca). DESPUÉS una **función nueva grande: planificación colaborativa de grupo** (`37c56c4` backend + `725680d` frontend) — el organizador crea un grupo y comparte un link, cada viajero añade SU ciudad; ver entrada "Hecho (25-jun)" abajo. Es la feature que ataca el cuello de botella (tráfico): cada miembro que abre el link es una visita. Resumen de la sesión previa 22-23 jun (todo en prod, verificado por contenido):
- **Pulido (22-jun)**, en commits sueltos a main: P1 fix del hero de convergencia (era una regresión real: salía casi invisible; ahora ESTÁTICO y fiable, ver "Diseño vigente") · P4 unificación de tokens (radios→`--radius-*`, sombras→`--shadow-sm`) · P3 UX de un solo origen (oculta toda la UI de equidad) · P2 reequilibrio del layout desktop (hero protagonista + guía rápida en la columna del selector) · consolidación de breakpoints (escala canónica + fix del solape 768) · **poda de 555 clases CSS muertas** (CSS −29% gzip, 39→27,7 KB).
- **Funciones nuevas (22-23 jun)**: **nudge "fecha más barata"** (`450ba1a`, ver entrada abajo) y **imagen OG dinámica al compartir** (`67d5ccf`, ver entrada abajo). Ambas verificadas en prod.

Detalle de cada cosa en las entradas "Hecho" de abajo. **No hay trabajo pedido pendiente.** Los datos/precios siguen fiables (el nudge reusa el calendario de Travelpayouts, sin inventar). Diagnóstico de fondo que SIGUE vigente: **el cuello de botella real no es producto ni datos, es TRÁFICO/usuarios** — sin visitas no hay reservas de afiliado ni sentido a verificar más. El kit y el tracker de outreach están en `Fyndme/` (junto al repo). Si Diego pregunta "qué mejorar": ofrecer crecimiento/distribución antes que más pulido (la shareability OG ya empuja en esa dirección). **Ya NO quedan follow-ups de pulido anotados**: la fuente latina con € real (Hecho 25-jun A), la cohesión de interacciones (B) y el **barrido completo del azul residual del tema viejo** (C, `423a012`) están todos hechos. El único azul que queda en el CSS es el de marca #0059B8 a propósito (botón "Copy link", fairness mid, `--info`).

Backlog técnico original VACÍO (capturas README, vite@8, SEO, analítica y promoción del precio verificado: todo hecho 16-jun). Quedan:
1. **Vigilancias pasivas** (sin fecha): capa 2 `[serpapi-verify]` en logs de Render + cupo serpapi.com; primeras reservas de afiliado en el dashboard de Travelpayouts; y ahora **Vercel Analytics** (búsquedas y `book_click`) cuando haya tráfico real.
2. **Candidatos de mejora abiertos**: verificar (capa 2) más allá del ganador vigilando el cupo de SerpAPI; WinnerCard más celebratorio (Diego lo descartó explícitamente — no retomar sin que lo pida).
3. **Ideas "qué implementar" (jun-2026)** alineadas con el alma del producto (grupo + equidad, datos honestos, coste 0). La #1 YA está hecha (ver "Hecho 25-jun, grupo colaborativo"). Quedan: **#2 aviso de bajada de precio** ("vigilad este destino", re-enganche — esfuerzo mayor: persistencia + email + re-chequeo honesto contra el calendario de Travelpayouts); **#3 round-trip + "semana más barata"** en el nudge de fecha (hoy solo ida — incremento de bajo riesgo). Endurecimiento del grupo: persistir el store (hoy in-memory, ver limitación en la entrada).

Hecho (25-jun-2026, función nueva grande: PLANIFICACIÓN COLABORATIVA DE GRUPO, en prod): commits `37c56c4` (backend) + `725680d` (frontend), directo a main, verificado end-to-end en prod. **Qué es**: el organizador pulsa "¿Lo planeáis entre varios? Invita a tu grupo" en el buscador → se crea un grupo (fecha + tipo de viaje + orígenes ya puestos) y aparece un enlace de invitación. Cada viajero abre `?group=<id>`, añade SU ciudad de salida (autocompletado ligero) y nº de viajeros; el roster se llena; cualquiera pulsa "Find the best destination" y corre la búsqueda normal (reutiliza TODO el flujo de resultados vía el patrón `pendingResearch`). **Por qué encaja**: la app optimiza la equidad de un GRUPO pero la entrada de datos no era de grupo; ahora sí. Y ataca el cuello de botella REAL (tráfico): cada miembro que abre el link es una visita nueva = distribución integrada. **Arquitectura** (incremental, no rewrite): backend `routes/groups.js` = recurso in-memory clon del `shareStore` (POST /api/groups · GET /:id · POST /:id/members · DELETE /:id/members/:index), TTL 14 días, rate-limit, cap 9, validación de fecha/miembro, soporta returnDate (ida y vuelta). NO computa ni guarda precios — solo orígenes; la búsqueda real sigue siendo /multi-origin (honesto). Frontend: `components/GroupPlanner.jsx` (vista nueva) + cableado en App.jsx (estado, carga por `?group=`, handlers) + botón en SearchPage + i18n EN/ES + CSS theme-aware. **OJO `?group=` no es `?trip=`** (este último ya era el tipo de viaje oneway/roundtrip, colisión evitada). **LIMITACIÓN honesta**: el store es in-memory (como el de share); con TTL de 14 días pero Render (free) reinicia en cada deploy/sleep → un grupo puede perderse si Render reinicia entre crear y usar. Sirve para VALIDAR la idea; el endurecimiento es persistirlo (mini-DB). Sin variables nuevas en prod (GROUP_CREATE_LIMIT/GROUP_MEMBER_LIMIT tienen default). **Bug cazado y arreglado durante la verificación**: el autocompletado ponía "Barce·lon·a" antes que "LON/London" al teclear "lon" (substring ganaba al código) → fix con ranking (código exacto/prefijo > ciudad prefijo > contiene). Backend 110/110 (10 tests nuevos), frontend 59/59, build OK, flujo completo verificado en navegador claro+oscuro sin errores JS, y E2E en prod (crear→añadir→leer + fecha inválida 400 + bundle Vercel con la i18n del grupo). Estructura: añade `backend/routes/groups.js`, `backend/test/groups.test.js`, `frontend/src/components/GroupPlanner.jsx`.

Hecho (25-jun-2026, tanda de pulido extra a petición de Diego, en prod): dos frentes, ambos verificados por contenido en prod. Commits `a17817b` (OG) + `bd3b29e` (pulido), directo a main.
- **(A) Fix OG: fuente con cobertura latina completa + € real** (`a17817b`). Sustituida la fuente bundleada por `frontend/public/fonts/PlusJakartaSans-Bold-latin.woff` (subset propio con `python -m fontTools.subset` de la TTF oficial: Latin + Latin-Extended + monedas/puntuación, 39,5 KB, 438 glifos con €/acentos/polaco-checo) y QUITADO el truco "EUR" de `frontend/api/og.js` (ahora pinta el `pp`/`total` con € directo). CDN fallback ampliado a latin-ext + latin. **Verificado en PROD (Edge)**: descargado el PNG real de `https://flyndme2.vercel.app/api/og?...` con € + ciudades acentuadas → muestra "Málaga", "€169 per person", "from Madrid, Düsseldorf, Kraków", "€508 ·" todos correctos (ya no "EUR", sin tofu). Ver la corrección del misdiagnóstico en la entrada del 23-jun. Frontend 59/59, backend 100/100, build OK. OJO de método: el render local con `@vercel/og` en Node NO es fiel (Node tiene fallback de sistema, Edge no) → usar `satori` directo con SOLO la fuente para reproducir Edge.
- **(B) Cohesión de interacciones (CSS, `bd3b29e`)** tras audit visual con capturas (home/resultados × claro/oscuro × desktop/móvil; veredicto: el producto ya estaba muy pulido, sin defectos visuales evidentes). Lo único concreto = **azul residual del tema viejo (#0062E3 ≡ `rgba(0,98,227)`) usado como acento sobre elementos granate/coral**. Alineado con la marca, todo verificado por probe del estilo computado en la app viva: pulso del CTA "Find destination" azul→**coral** (a juego con el botón, que computa #FF6B6B); halo de focus del input de ciudad y del checkbox/radio azul→**primario theme-aware**; destello de entrada de la WinnerCard (`pulseGlow`) azul→**granate theme-aware**; sombra hover muerta/override del botón primario azul→granate. Para los theme-aware se añadió token **`--primary-rgb`** (claro `174,47,52` / oscuro `255,179,176`) usado como `rgba(var(--primary-rgb), α)` → el halo de focus ahora SIGUE al borde en ambos temas (granate en claro, rosa en oscuro). Además **nuevo press `:active` en los botones de reserva `.wc-cta`** (Skyscanner/Google), que solo tenían hover — feedback táctil en los CTA de conversión. Frontend 59/59, build OK, solo CSS.
- **(C) Barrido completo del azul residual (CSS, `423a012`)**: las 14 instancias restantes de `rgba(0,98,227)` (#0062E3, azul del tema viejo) usadas como **tinte muy tenue de fondo/borde, `::selection`, y estados focus/hover/drag** → `rgba(var(--primary-rgb), α)` (granate en claro, rosa en oscuro, mismo alpha). Cubre orbe del hero (`.lp-hero::before`), `::selection`, `.sf-section:focus-within`, bordes de budget-box/loading-tip/wc-badge/stats-export, fondos de summary/weekday-badge/chips y hovers de breadcrumb/origin-row/quick-research. Confirmado que `var()` funciona en `::selection` (Chromium). **NO se tocó el azul de marca #0059B8** (botón "Copy link", fairness mid, `--info`) — es intencional. Verificado por capturas en claro + oscuro + selección de texto. 59/59, build OK.

Hecho (23-jun-2026, nueva función): **imagen OG dinámica por resultado** (en prod, main `67d5ccf`). Al compartir un resultado, el enlace previsualiza una tarjeta con el resultado real ("Cheapest place to meet · **Paris** · **169 EUR per person** · from Madrid, London, Berlin · Group total 508 EUR · 3 travelers") en vez de la imagen estática. Cadena: compartir → backend `/api/share/:id/og` pone `og:image` apuntando a `/api/og` con los datos del ganador → **función Edge `frontend/api/og.js` (`@vercel/og`)** renderiza un PNG 1200×630 desde query params. Fuente Jakarta Bold bundleada en `public/fonts` (same-origin + CDN fallback). Verificado en prod la cadena completa (share real → og:image `%E2%82%AC` → "169 EUR"). **DOS lecciones caras (mucho tiempo perdido)**: (1) [CORREGIDO el 25-jun, era un MISDIAGNÓSTICO] se creyó que la fuente bundleada (16 KB) no traía € ni acentos y por eso la función convertía el € a "EUR". En realidad la fuente SÍ tenía € y los acentos Latin-1 (verificado por cmap con fonttools); el único hueco real era Latin-Extended (polaco/checo). El "tofu" que se vio en prod muy probablemente fue la corrupción del € a U+FFFD de la lección (2), no la fuente. El 25-jun se sustituyó por un subset completo (Latin + Latin-Ext + monedas, 39,5 KB) y se quitó el truco "EUR" → renderiza el € REAL y ciudades con acento sin tofu. MÉTODO que lo zanjó: render fiel con `satori` usando SOLO la fuente (igual que Edge, sin fallback de sistema) → PNG → mirarlo; control con la fuente vieja confirmó que satori SÍ hace tofu visible con glifos ausentes (no inventa fallback), así que el test es real. (2) **`curl --data-urlencode "pp=€..."` corrompía el € a U+FFFD** (encoding del shell) → me hizo creer durante ~20 min que el deploy estaba roto cuando NO lo estaba; el flujo real del backend usa `%E2%82%AC` y funciona. Lección: para probar params con no-ASCII, usar el %-encoding literal (`%E2%82%AC`), no el carácter en el shell; y un header `x-og-version` temporal ayudó a distinguir "función no redesplegada" de "lógica mala". Frontend 59/59, backend 91/91 (test de `og:image`→`/api/og`). Vercel infra: añade función Edge en `frontend/api/` sin tocar variables de prod.

Hecho (22-jun-2026, nueva función): **nudge de "fecha más barata" para el grupo** (en prod, main `450ba1a`). Tras pintar resultados, en 2º plano (patrón de la verificación capa 2) se consulta `POST /api/flights/cheaper-date`: ¿hay una fecha cercana donde el GRUPO pague menos por el mismo destino ganador? Si el ahorro supera el umbral (`max(15€, 5%)`), aparece UN aviso bajo la WinnerCard ("Quedad el 21 sep y el grupo paga €376 — ahorráis €132") + botón "Usar esta fecha" (relanza la búsqueda con esa fecha vía flag+efecto). Solo ida (v1; roundtrip = espacio de 2 fechas). **Diseño con rigor (toca precios)**: lógica PURA en `services/cheaperDate.js` (`findCheaperGroupDate`: agrega por día, exige precio real para TODOS los orígenes el MISMO día —regla 1—, umbral de ahorro) con 7 tests unitarios; nuevo `getDatedPrices()` en la interfaz de proveedores (travelpayouts + mock, drop-in). **OJO coste**: la 1ª versión leía la caché por DÍA (que solo trae ~ese día) y el nudge no disparaba nunca — la verificación EN PROD lo cazó; el fix (`450ba1a`) consulta el CALENDARIO del mes (`departure_at=YYYY-MM` → el más barato de cada día) en los meses que toca la ventana (`DATE_NUDGE_DAYS=14`): 1-2 consultas/origen, cacheadas (Travelpayouts gratis/ilimitado), NO es coste 0. Verificado en prod: MAD→BCN sugiere 2-sep €43, MAD,BCN→PAR sugiere 23-sep €57; cuando no hay día común más barato devuelve `null` honestamente (p.ej. MAD/LON/BER→MIL, datos cacheados dispersos). Backend 100/100 (7 unit + 2 smoke), frontend 59/59, deploy verificado por contenido (Render commit + endpoint real + bundle Vercel con "Use this date"). NOTA: lección de método — un enriquecimiento que depende de datos del proveedor hay que verificarlo CONTRA EL PROVEEDOR REAL en prod, no solo con el mock (el mock generaba la ventana ±14 y ocultó la limitación del query por día).
Hecho (22-jun-2026): **tanda estética** (tras audit visual con capturas). **Hero v2 = firma de convergencia**: nuevo `ConvergenceHero.jsx` (varios orígenes MAD/LON/BER → un punto de encuentro, arcos granate animados con `prefers-reduced-motion`, sin precios inventados); hero a dos columnas (texto + diagrama), copy nueva en voz de usuario ("Where should you all meet?" / "¿Dónde quedáis todos?"), eyebrow redundante "✈ FlyndMe" eliminado. **Display font Bricolage Grotesque** solo en el H1 (`--font-display`, añadida a index.html junto a Jakarta). **Más granate**: acento del H1, eyebrow, orbs del hero retintados de azul/verde a granate/coral. **Iconos `lucide-react`**: los ~78 emojis de la UI sustituidos por iconos SVG coherentes (`currentColor`, regla `.lucide` para alineación inline); quedan emojis SOLO en i18n muerto no renderizado. **Precios `tabular-nums`** en WinnerCard/comparador/alternativas. 59/59 tests, build OK. Sin tocar datos/precios. **Segunda tanda (mismo día)**: microinteracciones del reveal de resultados (stagger de las filas `altl-row` —antes el stagger apuntaba a `.alt-card`, clase muerta—, hover por sombra en vez de transform para no chocar con el fill de la animación, focus-visible granate, `prefers-reduced-motion` respetado) + tratamiento editorial de las miniaturas de ciudad (`altl-thumb` con borde interior; placeholder sin foto = bloque con gradiente granate→lavanda + código, en vez de texto plano). WinnerCard NO tocado (Diego lo descartó explícitamente). 59/59 tests. **Tercera tanda (mismo día, tras audit del formulario/parte baja)**: COHESIÓN tipográfica = `--font-display` (Bricolage) extendido a TODOS los títulos de sección (`.sf-title`, `.lp-card-title`, `.lp-faq-title`, `.cmp-title`, `.fm-podium-title`) — antes solo el hero; formulario = afordancia "+" (`.sf-airport-add`, Lucide Plus) en cada fila del selector de aeropuertos, granate al hover; "How it works" = pasos con icono en círculo (MapPin/Search/Share2) en vez de número, y fix de una sombra azul residual del tema viejo en `.lp-step-num` → granate. Pendiente opcional: unificación de sombras/radios → **HECHA** en la sesión de pulido del 22-jun (ver entrada abajo).

Hecho (22-jun-2026, sesión de pulido tras nuevo audit visual con capturas): **P1 — fix del hero de convergencia (era una regresión real)**. El diagrama "firma" salía casi invisible en navegador REAL (verificado headed, no solo headless): solo el punto de destino, sin arcos ni nodos LON/BER. Causas: (a) los arcos usaban `stroke-dasharray:1` + `pathLength="1"` que Chromium NO normalizaba → línea de puntos de 1px = invisible; (b) los nodos eran blancos sobre lavanda (sin contraste). Al intentar arreglar la animación se descubrió el problema de fondo: **cualquier animación de entrada del SVG (stroke-dashoffset del arco, opacidad de nodos, o un fade del contenedor) se repinta mal de forma INTERMITENTE en Chromium** y deja el diagrama —o partes— invisible al azar entre cargas (reproducido ~10 veces, con `forwards` y con `backwards`, en claro y oscuro). Solución: diagrama **ESTÁTICO** (arcos sólidos `stroke-dasharray:320; stroke-dashoffset:0` sin animación; nodos con borde granate `--primary` para contraste; `pathLength` eliminado), conservando solo el pulso infinito del destino (`.cv-dest-ring`, fiable). Verificado 8/8 cargas en ambos temas. **P4 — unificación de tokens en `App.css`**: radios hardcodeados → `--radius-*` (exactos 8/12/16/20/999 con cero cambio de píxel; los intermedios off-scale redondeados hacia arriba por decisión de Diego: 10→sm(12), 14→md(16), ~57 esquinas; los radios diminutos 2-7px se dejan a propósito); la sombra `0 2px 8px rgba(0,0,0,.06)` ×12 → `var(--shadow-sm)` (ahora theme-aware, mejor en oscuro). **P3 — UX con un solo origen**: cuando `cleanOrigins.length <= 1` no hay dimensión de equidad (todos salen de la misma ciudad), así que se OCULTA toda la UI de equidad: anillo de fairness y toggle precio/equidad y barra+spread en WinnerCard, la `CostSplitCard` entera (App.jsx), el score de fairness del podio (`TopDestinationsPodium`) y la etiqueta de equidad del comparador (`CompareChart`; su banda de reparto ya se autooculta porque min==max). El multi-origen queda intacto (verificado con captura 1-origen vs 3-origen). Se conserva el "tu grupo ahorra €X vs la opción más cara" (compara destinos, sigue siendo válido). **P2 — reequilibrio del layout desktop**: (hero) la firma de convergencia ahora protagoniza — grid `0.95fr/1.05fr` a favor del visual y `.cv` max-width 440→560px (diagrama ~25% mayor, llena su columna); (formulario) la columna derecha del selector (`.sf-airports`) ya no queda vacía cuando la lista se filtra: lleva una "guía rápida" desktop-only (`.sf-aside-guide`, "En 3 pasos" con etiquetas CORTAS propias `search.asideGuideSteps`, no las frases largas de `landing.steps`, para no duplicar la sección "¿Cómo funciona?" de abajo). Oculta en móvil (donde la aside es drawer). **Consolidación de breakpoints**: la escala de `@media` se redujo a un set canónico (max-width 360/480/600/767.98/860/960 + min-width 768 + print/reduced-motion/hover). Snaps: 576→600 (4), 640→600 (1, eran skeletons), 560→600 en `results-simple.css`; y el boundary tablet arreglado — `max-width:767` y `max-width:768` → `767.98` y `min-width:769`→`768`, eliminando el solape REAL en el que `max-width:768` y `min-width:768` aplicaban a la vez a 768px exactos. Verificado en 620/767/768 (home+resultados) sin glitches. **Poda de CSS muerto en `App.css`**: 555 clases sin uso (las de los ~45 widgets de datos inventados podados en junio: co2/wifi/visa/season/donut/sparkline/timeline/countdown/fxcalc/ppkm/alt-card viejo, etc.). Detección con script propio robusto (uso literal en jsx/js/html + stem de sufijos dinámicos por dígitos + safelist de los 3 bases con sufijo interpolado `fm-toast--`/`fm-backend-dot--`/`sf-date-warn--` + cross-ref a otros .css). Eliminación vía PostCSS AST (no regex): borra solo los selectores que contienen una clase muerta, recorta selectores múltiples conservando los vivos, y purga `@media` que quedan vacíos → 973 reglas borradas, 7 selectores recortados, 15 `@media` vacíos. **CSS 236→139 KB en crudo, 39→27,7 KB gzip (−29%)**. Verificado por smoke visual exhaustivo (home/resultados claro+oscuro, móvil, comparar, mapa, single-origin) idéntico al estado previo: nada perdió estilo. 59/59 tests, build OK. Sin tocar datos/precios. NOTA de método: el audit con puppeteer headless dio un FALSO positivo inicial sobre el hero por el bug de repintado; **verificar siempre el render final de animaciones SVG en headed, no solo headless**.

Hecho (21-jun-2026): **recorte de peso del frontend** (`181925b`): fuera el import muerto de `bootstrap.bundle.min.js` (no se usa ningún componente JS de Bootstrap — cero `data-bs-*`/API JS) y el `bootstrap.min.css` completo sustituido por `src/styles/bootstrap-custom.scss` (subset Sass: solo buttons/button-group/card/badge/alert/spinners/form-control/grid + utilidades planas exactas; añadida devDep `sass`). El loader SSR de tests stubea `.scss/.sass`. Resultado: **CSS 60,1→37,9 KB gzip (−37%), JS 113,6→91,6 KB gzip (−19%)**, ~44 KB menos en la ruta crítica. **Rediseño de la pestaña Comparar** (`2790ff5`): el scatter precio-vs-fairness (poco legible) sustituido por un ranking de barras horizontales ordenado de más barato a más caro (escala compartida), con la equidad CONCRETA ("cada uno paga €min–€max" + banda en la misma escala, precios reales `flights[].price`) y etiqueta humana en vez del score 0-100; eliminado el SVG del scatter y su CSS `cc-*`. HTML/CSS accesible, modo oscuro vía tokens del sistema. i18n EN+ES a mano, 59/59 tests. Ambos commits directo a main, **deploy verificado en prod por contenido** (chunk `CompareChart` nuevo con `cmp-row`/`cmp-spread`, scatter `cc-quad`/`scaleX` desaparecidos, Popper de Bootstrap fuera, i18n nueva servida). OJO: los hashes de bundle de Vercel difieren de los del build local (mismo contenido, distinto entorno) → verificar deploys por contenido, no por hash.

Hecho (11-jun-2026): rediseño Stitch mergeado a main y rama borrada; Amadeus eliminado (código + variables Render); monetización completa y verificada en prod (buildAffiliateLink + CTA + marker activo, 6 tests); capa 2 SerpAPI implementada, desplegada y VERIFICADA en prod (badge ✓ confirmado por Diego con búsqueda real; fix aeropuertos reales porque Google Flights no acepta códigos de ciudad); UX tanda 1 (control de ordenación único, a11y AA, responsive 360px) y tanda 2 (modo oscuro Stitch completo, lista abierta al cambiar criterio, fix crash de favoritos) aprobadas por Diego.

Hecho (16-jun-2026): capturas del README (`7de73ab`): 3 PNG en `docs/screenshots/` (home, resultados, mapa; tema claro, 1x ~1 MB total) generadas con la app en modo mock y el banner PWA oculto, sección "Capturas" añadida al README. Verificado que las 3 dan 200 en raw.githubusercontent. Smoke visual del lote del 13-jun confirmado OK en prod por Diego. **Migración vite@5→@8** (rama `chore/vite8`): vite@8.0.16 (bundler rolldown, ya no esbuild/rollup) + @vitejs/plugin-react@6.0.2 + @babel/core@7.29.7; `npm audit` pasó de 3 vulns (2 high esbuild/vite dev-only + 1 low babel) a **0**. QA: build OK (rolldown, ~350 ms, code-splitting intacto), 57/57 tests, smoke en navegador de DEV y PREVIEW (build prod) — home + búsqueda completa renderizan sin errores de consola. El loader de tests usa `@babel/core` directo (independiente del bundler); plugin-react@6 ya no arrastra babel. **Fix SEO/dominio** (`03d376d`): `flyndme.vercel.app` daba 404; corregidas las 5 refs de index.html (canonical/og/twitter/JSON-LD) + default de `FRONTEND_URL` del backend a `flyndme2.vercel.app`, añadidos robots.txt/sitemap.xml + test de regresión; `FRONTEND_URL` puesta en Render. **Analítica Vercel Web Analytics**: `@vercel/analytics` + `<Analytics/>` en main.jsx + `utils/analytics.js` (track saneado); `trackEvent` reconectado (activa los ~7 eventos ya existentes) y nuevo evento **`book_click`** en los 6 CTAs de reserva (winner+alts × travelpayouts/skyscanner/google) + evento `search`. Embudo search→search_complete→book_click verificado en navegador. Web Analytics **HABILITADO por Diego** en el dashboard de Vercel y verificado en prod (`/_vercel/insights/script.js` da 200, beacon carga sin errores). OJO: el CTA Travelpayouts (`offer.link`) NO se renderiza en modo mock (lo añade el backend solo con provider real). **Promoción del precio verificado** (`76243f0`): cuando la verificación capa 2 es completa (verified/changed), el precio verificado pasa a ser el mostrado (ver sección Capa 2); solo frontend, 59/59 tests + smoke navegador interceptando `changed`, deploy en prod verificado (bundle nuevo + render OK).

Hecho (12/13-jun-2026): tanda 3 de fixes publicada (`e9b193b`: sección de compra desplegable —selectores BEM—, deep links Skyscanner/Google Flights con aeropuertos reales, formato yymmdd, botón muted legible en oscuro); favicons generados en frontend/public (todas las referencias de index.html/manifest resuelven); npm audit fix (backend a 0 vulnerabilidades; frontend solo quedan esbuild/vite dev-only); Landing/CostSplitCard ya estaban extraídos en commits previos — se podaron 11 imports muertos de App.jsx; micro-a11y completado (separadores `aria-hidden`, `.rv-tab-badge` AA en todos los estados —estaba roto por un `var(--slate-600)` inexistente—, useFocusTrap en favoritos/atajos/drawer móvil); mapa de destinos rediseñado con geodatos reales Natural Earth 50m (chunk lazy 23,5 KB gzip, claro+oscuro, arcos Bézier, clamp para ciudades fuera de bbox tipo TFS); manifest.json de la PWA pasado a colores Stitch (theme #AE2F34, fondo #FCF8FF).

## Agentes
Hay 5 subagentes en `.claude/agents/` (backend, frontend, qa, release, producto). Delega el trabajo en ellos según el área; qa valida antes de cualquier push.
