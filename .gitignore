# Dependencies
node_modules/
# Sin barra final: también ignora node_modules si es un SYMLINK (un symlink
# no es un directorio y el patrón con barra no lo cubre — así se colaron al
# repo los enlaces de desarrollo del sandbox en jun 2026)
backend/node_modules
frontend/node_modules

# Build output
dist/
build/

# Secrets — never commit
.env
.env.local
.env.*.local
.env.production

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Claude local (agentes y settings por máquina — copia maestra en Fyndme\claude-agents\)
.claude/
.agents/
skills-lock.json
CLAUDE.md.bak

# Carpeta de trabajo de sesiones Cowork (no es codigo del proyecto)
Fyndme/
